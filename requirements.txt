# ── MQTT ─────────────────────────────────────────────────────
paho-mqtt>=1.6.0

# ── HTTP (model download) ───────────────────────────────────
requests>=2.20.0

# ── Inference (system packages on Jetson Nano) ──────────────
# numpy and Pillow are NOT listed here because on Jetson Nano
# they must come from system packages (aarch64 wheels unavailable).
# Create venv with: python3 -m venv venv --system-site-packages
# System provides: numpy, Pillow (PIL), tensorrt, pycuda

# ── Utilities ───────────────────────────────────────────────
python-dotenv>=0.19.0
